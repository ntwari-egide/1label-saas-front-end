import { Mail, Home, CheckCircle, Circle } from "react-feather"

const menuArray = [
  {
    id: "home",
    title: "Home",
    icon: <Home size={20} />,
    navLink: "/home"
  }
]

export default menuArray
